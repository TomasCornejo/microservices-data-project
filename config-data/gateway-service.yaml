server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: relojLlegada
          uri: lb://relojLlegada
          predicates:
            - Path=/relojllegada/**
